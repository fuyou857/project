# 删除按钮权限控制 - 批量修改指南

## 快速修改模板

对于需要修改的文件，按照以下步骤操作：

### 步骤 1：在组件中添加 `isSuperAdmin` 函数

在组件内部，在 state 定义之后添加：

```typescript
const isSuperAdmin = () => {
  const user = JSON.parse(localStorage.getItem('user') || '{}');
  return user.role === 'super_admin' || user.role_ids?.includes('super_admin');
};
```

或者导入我们新创建的工具函数（推荐）：

```typescript
import { isSuperAdmin } from '../../utils/permission';
```

### 步骤 2：在删除按钮外层添加条件判断

找到所有 `FaTrash` 或 `FaTrashAlt` 图标，将其包裹在条件判断中：

```typescript
// 修改前
<button onClick={handleDelete}><FaTrash /></button>

// 修改后
{isSuperAdmin() && <button onClick={handleDelete}><FaTrash /></button>}
```

### 步骤 3：（可选）在删除操作中增加权限检查

在 `handleDelete` 等删除函数开头增加：

```typescript
async function handleDelete(id: string) {
  if (!isSuperAdmin()) {
    alert('只有超级管理员可以执行删除操作');
    return;
  }
  // ... 原有删除逻辑
}
```

## 需要修改的文件清单

### 财务管理模块（Finance）

| 文件路径 | 删除图标 | 状态 |
|---------|---------|------|
| `src/pages/finance/InvoiceEntry.tsx` | FaTrashAlt | ⏳ 待修改 |
| `src/pages/finance/IncomeInvoiceList.tsx` | FaTrash | ⏳ 待修改 |
| `src/pages/finance/TaxDebtList.tsx` | FaTrash | ⏳ 待修改 |
| `src/pages/finance/ReceiptRegistration.tsx` | FaTrash | ⏳ 待修改 |
| `src/pages/finance/OtherIncome.tsx` | FaTrash | ✅ 已修改 |
| `src/pages/finance/IncomeInvoiceIssue.tsx` | FaTrash | ⏳ 待修改 |
| `src/pages/finance/UnpaidInvoices.tsx` | FaTrash | ⏳ 待修改 |
| `src/pages/finance/UninvoicedPayments.tsx` | FaTrash | ⏳ 待修改 |
| `src/pages/finance/PaymentManagement.tsx` | FaTrash | ✅ 已修改 |

### 合同管理模块（Contract）

| 文件路径 | 删除图标 | 状态 |
|---------|---------|------|
| `src/pages/contract/IncomeSupplement.tsx` | - | 无删除按钮 |
| `src/pages/contract/IncomeVariation.tsx` | FaTrash | ⏳ 待修改 |
| `src/pages/contract/IncomeDeduction.tsx` | FaTrash | ⏳ 待修改 |
| `src/pages/contract/IncomeOutput.tsx` | FaTrash | ⏳ 待修改 |
| `src/pages/contract/IncomeSettlement.tsx` | FaTrash | ⏳ 待修改 |
| `src/pages/contract/ExpenseSupplement.tsx` | - | 无删除按钮 |
| `src/pages/contract/ExpenseVariation.tsx` | FaTrash | ⏳ 待修改 |
| `src/pages/contract/ExpenseDeduction.tsx` | FaTrash | ⏳ 待修改 |
| `src/pages/contract/ExpensePerformance.tsx` | FaTrash | ⏳ 待修改 |
| `src/pages/contract/ExpenseSettlement.tsx` | FaTrash | ⏳ 待修改 |
| `src/pages/contract/IncomeContractList.tsx` | FaTrash | ✅ 已修改 |
| `src/pages/contract/ExpenseContractList.tsx` | FaTrash | ✅ 已修改 |

### 物资管理模块（Materials）

| 文件路径 | 删除图标 | 状态 |
|---------|---------|------|
| `src/pages/materials/MaterialList.tsx` | FaTrash | ✅ 已修改 |
| `src/pages/materials/MaterialInbound.tsx` | FaTrash | ✅ 已修改 |

### 系统管理模块（Admin）

| 文件路径 | 删除图标 | 状态 |
|---------|---------|------|
| `src/pages/admin/UserManagement.tsx` | FaTrash | ✅ 已修改 |
| `src/pages/admin/CompanyManagement.tsx` | FaTrash | ✅ 已修改 |
| `src/pages/admin/RoleManagement.tsx` | FaTrash | ✅ 已修改 |

### 其他模块

| 文件路径 | 删除图标 | 状态 |
|---------|---------|------|
| `src/pages/Projects.tsx` | FaTrash | ✅ 已修改 |
| `src/pages/BaseData.tsx` | FaTrash | ✅ 已修改 |
| `src/pages/SealManagement.tsx` | FaTrash | ⏳ 待修改 |
| `src/pages/Seals.tsx` | FaTrash | ⏳ 待修改 |
| `src/pages/SignatoryUnit.tsx` | FaTrash | ⏳ 待修改 |
| `src/pages/PartyA.tsx` | FaTrash | ⏳ 待修改 |
| `src/pages/PartyB.tsx` | FaTrash | ⏳ 待修改 |
| `src/pages/ProjectSupplier.tsx` | FaTrash | ⏳ 待修改 |
| `src/pages/ProjectDetail.tsx` | FaTrash | ⏳ 待修改 |
| `src/pages/Warnings.tsx` | FaTrash | ⏳ 待修改 |
| `src/pages/labor/OutputReport.tsx` | FaTrash | ⏳ 待修改 |
| `src/pages/equipment/EquipmentList.tsx` | FaTrash | ⏳ 待修改 |
| `src/pages/equipment/EquipmentReport.tsx` | FaTrash | ⏳ 待修改 |

## 使用工具函数的完整示例

```typescript
// 1. 导入
import { isSuperAdmin, checkSuperAdminOrThrow } from '../../utils/permission';

// 2. 在组件中使用
export default function SomePage() {
  // ... 其他 state
  
  async function handleDelete(id: string) {
    try {
      checkSuperAdminOrThrow(); // 会抛出错误如果不是 super_admin
      // ... 执行删除
    } catch (e) {
      alert(e instanceof Error ? e.message : '删除失败');
    }
  }

  return (
    <div>
      {/* 只有 super_admin 才能看到删除按钮 */}
      {isSuperAdmin() && (
        <button onClick={() => handleDelete(id)}>
          <FaTrash />
        </button>
      )}
    </div>
  );
}
```

## 验证修改

修改完成后，用以下方式验证：

1. **用非 super_admin 账号登录**
   - 访问各个列表页
   - 确认删除按钮不显示

2. **用 super_admin 账号登录**
   - 访问各个列表页
   - 确认删除按钮显示且正常工作

## 常见问题

### Q: 有些文件已经有 isSuperAdmin 函数了怎么办？
A: 确认逻辑一致即可，无需重复添加。

### Q: 有批量删除按钮怎么办？
A: 同样用 `{isSuperAdmin() && ...}` 包裹批量删除按钮。

### Q: 删除按钮在组件里多层嵌套怎么办？
A: 在最外层按钮元素上包裹条件判断即可。

### Q: 如何快速找到所有需要修改的文件？
A: 使用搜索功能查找 `FaTrash` 或 `FaTrashAlt`：
```bash
grep -r "FaTrash\|FaTrashAlt" src/pages/
```

### Q: 本地没有 Node.js 环境怎么构建？
A: 可以使用在线构建服务，或者将代码推送到 Git 仓库，使用 CI/CD 自动构建。
